# Core runtime requirements
# Spark / Sail
pyspark==4.1.1
grpcio>=1.48.1
grpcio-status>=1.48.1
protobuf>=3.20.3
zstandard>=0.25.0
pysail==0.6.0
pyarrow>=19.0.0
pandas>=2.2.0
tabulate>=0.9.0

# Telemetry
psutil>=5.9.0

# Dataset
datasets>=2.20.0
huggingface-hub>=0.24.0
pyyaml>=6.0

# Modelling (laptop-safe versions; heavy installs are conditional)
# The following are commented-out so `pip install -r requirements.txt`
# works on any laptop. Install them manually for actual model runs:
#
#   pip install torch transformers sentence-transformers
#   # for GPU (H200) also:
#   pip install vllm accelerate bitsandbytes
#
# The code uses lazy `import torch` etc. — it will fall back to mock models
# automatically if these are not installed, so the scaffolding + W0 run fine
# on a bare install.

# Plotting
matplotlib>=3.7.0
numpy>=1.24.0

# Testing / dev
pytest>=7.4.0
pytest-timeout>=2.2.0
maturin>=1.8.2
